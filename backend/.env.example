PORT=5000
MONGO_URI=mongodb://mongo:27017/hangman

# Optional. Defaults to reflecting whatever origin calls the API, so a
# separately hosted frontend works without extra setup. Set this only if
# you want to lock CORS down to one specific frontend origin.
# CLIENT_URL=https://your-frontend.example.com
